# Docker Compose configuration. Generate each key separately:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
ENCRYPTION_KEY=
# Required for the first standard-mode bootstrap; no built-in credentials.
SUPERADMIN_USERNAME=
SUPERADMIN_EMAIL=
SUPERADMIN_PASSWORD=
